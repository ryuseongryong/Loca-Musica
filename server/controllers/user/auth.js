const db = require('../../db');
const {
  isAuthorized,
  generateAccessToken,
  resendAccessToken,
  checkRefeshToken,
} = require('../tokenFunctions');

module.exports = {
  get: (req, res) => {},
};
